import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

// it's better to keep parent component simple and clean.
ReactDOM.render(<App />, document.getElementById('root'));
